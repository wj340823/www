<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport"
        content="initial-scale=1, maximum-scale=1, user-scalable=no, width=device-width,viewport-fit=cover">
    <title>罐信息</title>
    <link rel="manifest" href="manifest.json">
    <link rel="stylesheet" href="css/ionic.app.min.css">
    <!-- <script src="https://cdn.staticfile.org/angular.js/1.4.6/angular.min.js"></script> -->
    <script src="js/angular.min.js"></script>
</head>
<style>
    html {
        /*font-size: 75px;*/
    }

    header {
        font-size: 0.66rem;
    }

    body {
        background: #212429;
        /*color: #ffcc01;*/
        color: #ffffff;
        font-size: 0.4rem;
        padding: 15px;
    }

    h1 {
        font-size: 0.6rem;
        text-align: left;
        color: #ffcc01;
    }

    h2 {
        font-size: 0.44rem;
        text-align: left;
        color: lightgrey;
        opacity: 1;
        padding-bottom: 6px;
        border-bottom: 1px solid #ffffff;
    }

    h3 {
        font-size: 0.36rem;
        color: #ffcc01;
        text-align: center;
    }

    ul {
        padding: 0px;
    }

    li {
        list-style: none;
        margin-top: 10px;
    }

    .grey {
        color: lightgrey;
    }

    .noData {
        color: red;
        text-align: center;
    }

    table {
        width: 100%;
        border-collapse: collapse;
    }

    table td,
    table th {
        border: 1px solid #ffffff;
        text-align: center;
        font-size: 0.3rem;
    }

    thead th {
        color: lightgrey;
    }

    .intro {
        margin-top: 30px;
        border: 1px solid #ffffff;
        padding: 0.5rem;
        border-radius: 0.1rem;
    }

    .intro div {
        margin-bottom: 0.1rem;
    }
</style>

<body>
    <div ng-app="myApp" ng-controller="myCtrl">
        <header>{{page.data.qymc}}&nbsp;—&nbsp;{{page.data.jarname}}</header>
        <div class="c intro">
            <div>主要原材料：{{page.data.main}}

            </div>
            <div>
                最大储量：{{page.data.most}}
            </div>
            <div>
                常备储量：{{page.data.use}}
            </div>
            <div>
                责任人：{{page.data.person}}
            </div>
            <div>
                联系电话：{{page.data.contact}}
            </div>
            <div>
                编号：{{page.data.code}}
            </div>
        </div>
        <div ng-repeat="(key, val) in page.data.contentWithKey">
            <div class="c  lhtx" ng-if="key=='理化性质'">
                <h1>理化性质</h1>
                <h2></h2>
                <ul>
                    <li ng-repeat="it in val">
                        <span class="grey">{{it.key}}</span> <span ng-repeat="x in it.value">{{x}}</span>
                    </li>

                </ul>
            </div>
            <div class="c" ng-if="key!='理化性质'">
                <h1>{{key}}</h1>
                <div ng-repeat="it in val">
                    <h2>{{it.key}}</h2>
                    <ul>
                        <li ng-repeat="x in it.value">
                            {{x}}
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="c" ng-repeat="(key, val) in page.data.contentInfo">
            <h1>{{key}}</h1>
            <h2></h2>
            <ul>
                <li ng-repeat="it in val">
                    {{it}}
                </li>
            </ul>
        </div>
        <div class="c">
            <h1>应急保障</h1>
            <div ng-repeat="item in page.table track by $index">
                <h3>{{item.name}}</h3>
                <table>
                    <thead>
                        <tr ng-if="item.name=='主要人员构成表'">
                            <th ng-repeat="it in item.keyArr track by $index" colspan="{{$index==1?'2':'1'}}">
                                {{it}}</th>

                        </tr>
                        <tr ng-if="item.name!='主要人员构成表'">
                            <th ng-repeat="it in item.keyArr track by $index">
                                {{it}}</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr ng-repeat=" x in item.dataArr" ng-if="item.name=='主要人员构成表'">
                            <td ng-repeat="z in x track by $index" ng-if="!($index==0)"
                                rowspan="{{$index==1||$index==2?(x[0]+''):'1'}}">
                                {{z}}</td>
                        </tr>
                        <tr ng-repeat=" x in item.dataArr" ng-if="item.name!='主要人员构成表'&&item.name!='应急物资储备清单'">
                            <td ng-repeat="z in x track by $index">
                                {{z}}</td>
                        </tr>
                        <tr ng-repeat=" x in item.dataArr" ng-if="item.name=='应急物资储备清单'">
                            <td ng-repeat="z in x track by $index" ng-if="!($index==0||$index==1)"
                                rowspan="{{$index==6?(x[0]+''):$index==7?(x[1]+''):1}}">
                                {{z}}</td>
                        </tr>
                    </tbody>
                </table>
            </div>


        </div>

    </div>
</body>

<script type=" text/javascript"> function w() { var dom=document; var r=dom.documentElement; var
                                a=r.getBoundingClientRect().width; if (a> 750) {
                                a = 750;
                                }
                                var rem = a * 75 / 750;
                                r.style.fontSize = rem + 'px';
                                }
                                w();
                                </script>
<script>
    var app = angular.module('myApp', []);
    app.controller('myCtrl', function ($scope, $q, $timeout, $http) {
        var id = location.search.split('&')[0].split("=")[1];

        $scope.page = {
            data: {},
            table: [],
        }
        $scope.getJson = function () {
            $http.get("./potInfo2data.json").then(function (data) {

                if (id == 0) {
                    $scope.page.data = data.data.data[0];
                }
                if (id == 1) {
                    $scope.page.data = data.data.data[1];
                }

                $scope.page.table = data.data.data[0].contentTable;
            })
        }
        $scope.getRowSpan = function (item, x, z) {
            if (item.name == "主要人员构成表") {
                if (z == 0 || z == 1) {
                    return x[0];
                } else {
                    return 1;
                }
            }
        }
        $timeout(function () {
            $scope.getJson();
        })
    });
</script>

</html>