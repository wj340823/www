<div ng-controller="emerCaseCtrl">
    <div style="background-color: rgba(0,0,0,.7);padding: 12px 20px 20px">
        <div class="clearfix">
            <div class="tb-title"> 案例详情</div>
            <div class="fr">
                <button class="searchBtn" onclick="history.go(-1)">
                    <i class="iconfont icon-fanhui"></i>
                    返回
                </button>
            </div>
        </div>
        <div class="item-title">基本信息</div>
        <div class="item-content">
            <p><span class="item-content-name">案例名称：</span>{{file.name}}</p>
            <p><span class="item-content-name">行政区划：</span>{{file.area }}</p>
            <p>
                <span  class="item-content-name">案例日期：</span>{{file.time| date:'yyyy-MM-dd'}}
            </p>
        </div>
        <div class="item-title">案例附件
            <button class="searchBtn" ng-hide="userList.role == 'szrole'"
                    style="float: right;margin-right: 10px;margin-top: 4px" ui-sref="emergency.upload({id:file.id})">
            <i class="iconfont icon-tianjia" style="font-size: 14px"></i>添加
            </button>
            <button class="searchBtn" ng-show="userList.role == 'szrole'"
                    style="float: right;margin-right: 10px;margin-top: 4px" ui-sref="cityEmerCase.upload({id:file.id})">
                <i class="iconfont icon-tianjia" style="font-size: 14px"></i>添加
            </button>
        </div>
        <div class="item-content">
            <table>
                <thead>
                <tr>
                    <th >序号</th>
                    <th >附件名称</th>
                    <th >附件类型</th>
                    <th >文件大小</th>
                    <th >上传日期</th>
                    <th >操作</th>
                </tr>
                </thead>
                <tbody>
                <tr ng-repeat="item in file.list">
                    <td>{{file.pageSize*(file.pageNo-1)+$index+1}}</td>
                    <td>{{item.NAME}}</td>
                    <td>{{item.TYPE==1?'现场照片':item.TYPE==2?'监测报告':item.TYPE==3?'工作方案':item.TYPE==4?'会议纪要':'其他'}}</td>
                    <td>{{(item.SIZE/1024/1024).toFixed(2)}}Mb</td>
                    <td>{{item.LASTUPDATETIME | date: 'yyyy-MM-dd'}}</td>
                    <td >
                        <a class="oper hander" ng-href="emergencyExample/downExampleAttachment?attachmentId={{item.ID}}">下载</a>
                        <span class="oper hander" ng-click="delFile(item)" >删除</span>
                    </td>
                </tr>
                </tbody>
            </table>
            <uib-pagination
                    ng-change="getFile()"
                    total-items="file.totalCount"
                    items-per-page="file.pageSize"
                    ng-model="file.pageNo"
                    class="pagination"
                    boundary-link-numbers="true"
                    direction-links="false"
                    rotate="false"
                    max-size="3"
                    first-text="首页"
                    last-text="末页">
            </uib-pagination>
            <div class="clearfix"></div>
        </div>

    </div>

</div>