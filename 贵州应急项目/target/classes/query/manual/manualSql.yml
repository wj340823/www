---
  grammer: SQL
  queries:
    #区域风险源排行
    - name: getLaws
      resultType: Map
      fetches: []
      statement:
        "
        select
          OBJECTID,NAME,FILENAME,LAWSTYPE
        from
          LawsRegulations
        where
          1 = 1
        #if($lawsType)
          and lawsType = :lawsType
        #end
        #if($name)
          and name like :name
        #end
        "
   