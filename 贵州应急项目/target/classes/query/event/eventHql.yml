---
  grammer: HQL
  queries:
    #查询模拟事件
    - name: getEvent
      resultType: AtmosphericEvent
      fetches: []
      statement:
        "
        from
          AtmosphericEvent
        where
          1=1
        #if($simulationId)
          and simulationId = :simulationId
        #end
        #if($companyId)
          and companyId = :companyId
        #end
        #if($name)
          and name like :name
        #end
        "
